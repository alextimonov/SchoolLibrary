<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
        xmlns="http://www.springframework.org/schema/security"
        xmlns:beans="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:security="http://www.springframework.org/schema/security"

        xsi:schemaLocation="
            http://www.springframework.org/schema/security
            http://www.springframework.org/schema/security/spring-security.xsd

		    http://www.springframework.org/schema/beans
		    http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!--<security:http create-session="stateless" pattern="/**">
        <security:intercept-url pattern="/**" access="ROLE_ADMIN"/>
        <security:http-basic/>
    </security:http>-->

    <security:global-method-security pre-post-annotations="enabled"/>

    <security:http auto-config="true" use-expressions="true">
        <security:intercept-url pattern="/library/protected/**" access="hasRole('ROLE_ADMIN')"/>
        <!--<security:intercept-url pattern="/**" access="hasRole('ROLE_USER')"/>-->
        <security:form-login/>
        <security:logout/>
    </security:http>

    <security:authentication-manager>
        <security:authentication-provider>
            <security:user-service>
                <security:user name="admin" password="admin" authorities="ROLE_ADMIN"/>
                <!--<security:user name="user" password="user" authorities="ROLE_USER"/>-->
            </security:user-service>
        </security:authentication-provider>
    </security:authentication-manager>

    <!--<http auto-config="true" use-expressions="true">
        <intercept-url pattern="/library/protected/**" access="hasRole('ROLE_ADMIN')"/>
        <form-login/>
        <logout/>
    </http>-->

    <!--<http pattern="/logout/user/" security="none"/>

    <http auto-config="true" use-expressions="true">
        <csrf disabled="true"/>
        <intercept-url pattern="/library/protected/**" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/login" access="isAnonymous()"/>
        <form-login login-page="/library/security/signin"
                    default-target-url="/library"
                    authentication-failure-url="/library/security/signin-failure"/>
        <logout logout-url="/logout" logout-success-url="/index.jsp"/>
    </http>
-->
    <!--<authentication-manager>
        <authentication-provider>
            <user-service>
                <user name="admin" password="admin" authorities="ROLE_ADMIN"/>
            </user-service>
        </authentication-provider>
    </authentication-manager>-->

</beans:beans>